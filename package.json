{
  "name": "flick-core",
  "version": "1.0.0",
  "description": "Flick service",
  "type": "module",
  "scripts": {
    "start": "node -r dotenv/config --experimental-specifier-resolution=node src/index.js",
    "start-dev": "nodemon -r dotenv/config --experimental-specifier-resolution=node src/index.js",
    "clean": "node -r dotenv/config --experimental-specifier-resolution=node src/script/clean.js",
    "populate": "node -r dotenv/config --experimental-specifier-resolution=node src/script/populate.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.6.0"
  },
  "engines": {
    "node": "16.4.2"
  },
  "devDependencies": {
    "eslint": "^7.30.0",
    "nodemon": "^2.0.12"
  }
}
